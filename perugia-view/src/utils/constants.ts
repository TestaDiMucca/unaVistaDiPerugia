/** Different full screens that are available */
export enum Views {
  /** Nothing is loaded */
  home = 'home',
  /** Images are loaded */
  show = 'show',
}

export const COLORS = {
  darkGray: '#161414',
};
